import 'package:flutter/material.dart';

class CategoryModel {
  String image;
  String title;
  Color color;
  CategoryModel({
    required this.image,
    required this.color,
    required this.title,
  });
}
